SlimX-RAG tiny demo
===================

SlimX-RAG is a deterministic retrieval demo for local knowledge bases.
It ingests Markdown files, chunks them, embeds them with the hash provider,
and writes a local index for retrieval tests.
