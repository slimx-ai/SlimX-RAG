# SlimX Core

SlimX Core is a lightweight Python runtime for provider-neutral LLM calls. It provides explicit request and response objects, streaming, structured output, tools, retries, and provider adapters.

SlimX Core treats providers as adapters rather than architecture. This helps applications move across OpenAI, Anthropic, Ollama, and future providers without rewriting the application flow.
